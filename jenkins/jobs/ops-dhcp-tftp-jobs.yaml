- job-template:
    name: 'ops-dhcp-tftp-check-{platform}'
    node: openswitch
    wrappers:
        - build-timeout:
            timeout: 120
    builders:
        - revoke-sudo
        - module-build-branch:
            module: 'ops-dhcp-tftp'
            platform: '{platform}'
    publishers:
        - post-ws-clean

- job:
    name: 'ops-dhcp-tftp-gate-as5712'
    node: openswitch

    wrappers:
        - build-timeout:
            timeout: 180

    builders:
        - module-build-branch:
            module: 'ops-dhcp-tftp'
            platform: 'as5712'
    publishers:
        - post-ws-clean

- job:
    name: 'ops-dhcp-tftp-gate-genericx86-64'
    node: vsi

    wrappers:
        - build-timeout:
            timeout: 180

    builders:
        - module-build-branch:
            module: 'ops-dhcp-tftp'
            platform: 'genericx86-64'
        - topology_test:
            module: 'ops-dhcp-tftp'
            platform: 'genericx86-64'
            test: 'component'
        - lcov-compile:
            module: 'ops-dhcp-tftp'
        - lcov-coverage-run:
            module: 'ops-dhcp-tftp'

    publishers:
#        - conditional-junit
#        - post-docker-clean
#        - conditional-htmlreports
#        - post-ws-clean
        - publish-topologyTest-results

- job-group:
    name: 'ops-dhcp-tftp-jobs'
    jobs:
      - 'ops-dhcp-tftp-check-{platform}'
